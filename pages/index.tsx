import Header from '@/components/header/Header';
import { NextPage } from 'next';

interface Props {}

const Home: NextPage<Props> = ({}) => {
  return (
    <>
      <Header />
    </>
  );
};

export default Home;
